# --- tgcloud / secrets -------------------------------------------------------
# CLI state: contains .tgcloud/credentials (your app<id>:<secret> access token).
.tgcloud/

# Local environment files. NEVER commit these — see .env.example for the shape.
.env
.env.*
!.env.example

# --- node --------------------------------------------------------------------
node_modules/
npm-debug.log*
yarn-error.log*
.npm/

# --- os / editor -------------------------------------------------------------
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# --- scratch -----------------------------------------------------------------
*.local
tmp/
